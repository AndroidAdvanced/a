package com.example.rupal.a.utils;

import android.app.Activity;


public class FbTemplate {

    private static FbTemplate facebook = null;

    static Activity mActivity = null;


    private FbTemplate(Activity paramActivity)
    {
//        mActivity = paramActivity;
//        if (this.session == null) {
//            this.session = Session.getActiveSession();
//        }
//        this.constants = Constants.getInstance();
    }

  void login() {
//        profile = ContactsContract.Profile.getCurrentProfile().getCurrentProfile();
//        if (profile != null) {
//            // user has logged in
//
//        } else {
//            // user has not logged in
//
//        }

     // mCallbackManager = CallbackManager.Factory.create();
    }
}
